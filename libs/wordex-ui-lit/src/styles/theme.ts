import { css } from 'lit';

export const theme = css`
  :host {
    --wordex-violet: #7c3aed;
    --wordex-cyan: #06b6d4;
    --wordex-bg: #130B2E;
    --wordex-text: #ffffff;
    --wordex-glass: rgba(255, 255, 255, 0.1);
    --wordex-border: rgba(255, 255, 255, 0.2);
    --wordex-radius: 12px;
    --wordex-transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }
`;
